## 1. Implement Dynamic Emojis in Route Selector

- [x] 1.1 Update `src/app/page.tsx` mobile layout header labels for Origen and Destino to display `📍` or `🏔️` dynamically based on `selectedDirection`.
- [x] 1.2 Update `src/app/page.tsx` desktop layout header labels for Origen and Destino to display `📍` or `🏔️` dynamically based on `selectedDirection`.

## 2. Verification

- [x] 2.1 Verify local build (`npx tsc --noEmit`) to ensure no syntax or JSX errors.
- [x] 2.2 Test direction swap behavior (SUBIDA ➔ BAJADA) to confirm emojis in Origen/Destino switch correctly in both Mobile and Desktop views.
