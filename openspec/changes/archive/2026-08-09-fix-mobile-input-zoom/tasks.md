## 1. Global CSS Styling

- [x] 1.1 Add `@media (max-width: 768px)` rule for `input`, `select`, and `textarea` with `font-size: 16px !important` in `src/app/globals.css`.

## 2. Verification

- [x] 2.1 Verify mobile input rendering and confirm no auto-zoom occurs on input focus in mobile viewports.

