## 1. CSS & Token Refinements

- [x] 1.1 Update `.glass-card` styling in `src/app/globals.css` with translucent background fills, backdrop blur (`backdrop-filter: blur(16px)`), specular inner border reflections (`inset 0 1px 0 rgba(255,255,255,0.18)`), and soft hover highlights.
- [x] 1.2 Add ambient background glow radial sources in `src/app/layout.tsx` or background containers so the frosted glass cards visually refract background light spots.

## 2. Card Components & Modals Refinement

- [x] 2.1 Refine `TripCard.tsx` and `TripCardAccordion.tsx` to apply `.glass-card` classes, smooth border transitions, and rounded card styling.
- [x] 2.2 Refine modal popups (`PublishModal.tsx`, `ProfileModal.tsx`, `TripDetailModal.tsx`) to use frosted glass overlays and surface containers.

## 3. Verification & Visual Quality Check

- [x] 3.1 Run build / dev server checks and verify text contrast readability, glass refraction aesthetics, and hover interactions.
