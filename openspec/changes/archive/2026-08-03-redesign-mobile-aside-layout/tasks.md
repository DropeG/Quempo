## 1. Mobile Layout Redesign (Contenedor Aside)

- [x] 1.1 Modificar clases responsive en `src/app/page.tsx` para cambiar el contenedor de `grid grid-cols-2 gap-3` a `flex flex-col gap-3 lg:gap-4` en móviles (< 1024px).

## 2. Optimización Visual de Cards (Mobile UI & Comfort)

- [x] 2.1 Implementar diseño dual en Panel 1 ("📍 Ruta de Viaje"): barra horizontal ultra-compacta para mobile (< 1024px) y cajas apiladas verticales en desktop (≥ 1024px).
- [x] 2.2 Reestructurar la card de Panel 2 ("¿Conduces a la cordillera?") a un banner horizontal equilibrado con botón CTA `Publicar Mi Viaje` a ancho completo.

## 3. Verificación

- [x] 3.1 Validar que la compilación de la app sea exitosa (`npm run build`).
