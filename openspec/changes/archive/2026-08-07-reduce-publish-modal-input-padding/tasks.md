## 1. Input Padding Adjustment

- [x] 1.1 Modificar la clase de padding izquierdo de `pl-7 sm:pl-8` a `pl-6 sm:pl-7` en los inputs de Fecha y Hora de Salida en `src/components/PublishModal.tsx`.
- [x] 1.2 Modificar la clase de padding izquierdo de `pl-7 sm:pl-8` a `pl-6 sm:pl-7` en los inputs de Asientos y Precio en `src/components/PublishModal.tsx`.

## 2. Verification

- [x] 2.1 Verificar que en vista móvil los placeholders de fecha y hora se desplieguen sin chocar con el borde derecho del campo.
