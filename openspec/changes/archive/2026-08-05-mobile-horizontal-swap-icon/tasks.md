## 1. Icon Import Update

- [x] 1.1 Import `ArrowLeftRight` alongside `ArrowUpDown` from `lucide-react` in `src/app/page.tsx`.

## 2. Mobile Layout Update

- [x] 2.1 Replace `<ArrowUpDown>` with `<ArrowLeftRight>` inside the mobile direction swap button component (`lg:hidden` container) in `src/app/page.tsx`.

## 3. Verification

- [x] 3.1 Verify mobile layout (`< lg` viewport) displays horizontal swap arrows (`ArrowLeftRight`).
- [x] 3.2 Verify desktop layout (`≥ lg` viewport) continues to display vertical swap arrows (`ArrowUpDown`).
- [x] 3.3 Verify direction swap action and 180-degree rotation animation work seamlessly on tap/click.
